{
  "name": "hydroponics-jma-weather",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "backup:run": "node scripts/backup-d1-to-r2.mjs",
    "backfill:thingspeak": "node scripts/backfill-thingspeak-to-d1.mjs",
    "credential:create": "node scripts/create-device-credential.mjs",
    "dev": "wrangler dev",
    "build:journal-images": "node scripts/build-journal-images.mjs",
    "deploy": "pnpm run build:journal-images && wrangler deploy",
    "restore:verify": "node scripts/restore-d1-backup.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "@jsquash/webp": "1.5.0",
    "esbuild": "0.25.9",
    "wrangler": "^4.0.0"
  }
}
